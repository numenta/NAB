# This file contains the known minimum and maximum values for several types
# of data sources contained in the NAB corpus.
#
# The format of this map is <string>:
#                               "min": <float>
#                               "max": <float>
#
# Data files in the corpus identify themselves as being part of each of these
# data types by including <string> in their filenames.
#
# e.g. ec2_cpu_utilization_24ae8d.csv contains "cpu_utilization"

slorm_cpu_utilization:
    min: 0.0
    max: 100.0
